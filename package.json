{
  "name": "rightsnow",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["apps/web", "api", "packages/*"],
  "description": "UK employment law assistant — Gov APIs first, AI fallback, cached answers",
  "scripts": {
    "dev": "npm run dev -w @rightsnow/web",
    "build": "npm run build -w @rightsnow/shared && npm run build -w @rightsnow/web",
    "start": "npm run start -w @rightsnow/web",
    "lint": "npm run lint -w @rightsnow/web",
    "typecheck": "npm run typecheck -w @rightsnow/web",
    "format": "prettier --write .",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "test:ci": "jest --coverage --passWithNoTests",
    "ci": "npm run lint && npm run typecheck && npm run test && npm run build -w @rightsnow/shared && npm run build -w @rightsnow/web",
    "build:packages": "npm run build -w @rightsnow/shared && npm run build -w @rightsnow/legal-core",
    "build:api": "npm run build -w @rightsnow/api",
    "build:web": "npm run build -w @rightsnow/shared && npm run build -w @rightsnow/web"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "@testing-library/jest-dom": "^6.5.0",
    "@testing-library/react": "^16.0.1",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.14.10",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "prettier": "^3.3.3",
    "typescript": "^5.5.4"
  },
  "engines": {
    "node": ">=20"
  }
}
