apiVersion: v1
kind: Service
metadata:
  name: iterlaw-postgres
  namespace: iterlaw
  labels:
    app.kubernetes.io/name: iterlaw-postgres
    app.kubernetes.io/part-of: iterlaw
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: iterlaw-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      protocol: TCP
