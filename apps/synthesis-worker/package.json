{
  "name": "@ordinoxai/synthesis-worker",
  "version": "0.0.1",
  "private": true,
  "description": "OrdinoxAI synthesis worker — internal queue consumer for legal-orchestrator draft synthesis (ADR 004 skeleton, no model client yet)",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "typescript": "^5.5.4",
    "vitest": "^3.1.3"
  }
}
