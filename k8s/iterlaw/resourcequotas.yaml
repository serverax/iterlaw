apiVersion: v1
kind: ResourceQuota
metadata:
  name: iterlaw-quota
  namespace: iterlaw-ai
spec:
  hard:
    requests.cpu: "4"
    requests.memory: 8Gi
    limits.cpu: "8"
    limits.memory: 16Gi
    pods: "20"
    services: "10"
    persistentvolumeclaims: "4"
