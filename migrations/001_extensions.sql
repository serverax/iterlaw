-- RightsNow Phase 0 Step 3 — extensions
-- pgvector: semantic similarity on questions.question_embedding
--
-- Optional (DB unit tests): run in SQL editor if your plan supports it:
--   CREATE EXTENSION IF NOT EXISTS pgtap;

CREATE EXTENSION IF NOT EXISTS vector;
