apiVersion: v1
kind: LimitRange
metadata:
  name: iterlaw-ai-limits
  namespace: iterlaw-ai
spec:
  limits:
    - type: Container
      default:
        cpu: "1"
        memory: "1Gi"
      defaultRequest:
        cpu: "100m"
        memory: "256Mi"
