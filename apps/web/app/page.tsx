export default function HomePage() {
  return (
    <main style={{ padding: '2rem', fontFamily: 'system-ui, sans-serif', maxWidth: '42rem' }}>
      <h1>RightsNow</h1>
      <p>
        UK employment law assistant for workers in workplace disputes. Gov APIs first, AI only when
        needed — Phase 0 scaffold. Configure <code>.env.local</code> and see{' '}
        <code>README.md</code> for architecture.
      </p>
    </main>
  );
}
