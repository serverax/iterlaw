{
  "name": "@rightsnow/ai-orchestrator",
  "version": "0.1.0",
  "private": true,
  "description": "IterLaw AI orchestrator — mock legal pipeline (AEE → ART → Risk → LVC)",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "start": "node dist/server.js",
    "dev": "tsx src/server.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "helmet": "^8.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.10",
    "tsx": "^4.21.0",
    "typescript": "^5.5.4",
    "vitest": "^3.1.3"
  }
}
