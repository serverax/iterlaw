import { VersionRegistry } from "./versionRegistry.js";

/** Versioned prompt strings (Sprint 19). */
export class PromptRegistry extends VersionRegistry {}

/** Versioned rule packs (Sprint 19). */
export class RuleRegistry extends VersionRegistry {}
