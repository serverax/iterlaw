# Disabled standalone IterLaw manifests

These manifests were created by the earlier master order using a standalone `iterlaw` namespace.

They are disabled because the canonical IterLaw architecture uses the existing multi-namespace layout:

- iterlaw-ai
- iterlaw-rag
- iterlaw-api
- iterlaw-monitoring
- iterlaw-security

Do not apply these manifests unless the architecture is explicitly changed.
