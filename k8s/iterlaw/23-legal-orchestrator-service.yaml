apiVersion: v1
kind: Service
metadata:
  name: iterlaw-legal-orchestrator
  namespace: iterlaw
  labels:
    app.kubernetes.io/name: iterlaw-legal-orchestrator
    app.kubernetes.io/part-of: iterlaw
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: iterlaw-legal-orchestrator
  ports:
    - name: http
      port: 3012
      targetPort: 3012
      protocol: TCP
