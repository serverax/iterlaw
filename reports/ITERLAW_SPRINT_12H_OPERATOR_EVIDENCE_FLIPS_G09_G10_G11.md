# Sprint 12H — Operator Evidence Flips (G09 / G10 / G11) Report

## STATUS: PARTIAL

Same two operator-environment blockers as Sprint 12F. Re-probed fresh:

- Docker Desktop daemon still not running on this workstation (`docker version` → daemon unreachable).
- SSH to `root@138.201.253.56` denied by the Claude Code permission classifier without explicit operator authorisation.

Sprint 14 + Sprint 15 scripts remain committed and ready for an operator-authorised run. **G09 / G10 / G11 remain `NOT_VERIFIED`** with refreshed blocker text. No fake PASS. No simulated remote output.

---

## 1. Docker evidence (re-probed for Sprint 12H)

```
$ docker version
... OS/Arch: windows/amd64 ...
... Context: desktop-linux ...
failed to connect to the docker API at npipe:////./pipe/dockerDesktopLinuxEngine; check if the path is correct and if the daemon is running: open //./pipe/dockerDesktopLinuxEngine: The system cannot find the file specified.
exit 0 (client only; daemon unreachable)
```

Sprint 14's `scripts/operator/sprint14-docker-staging-migration-replay.ps1` correctly refuses to continue without a working daemon (its pre-flight emits `FAIL: docker is not available` and returns exit 20). The script was not executed for Sprint 12H because the daemon precondition is unmet.

## 2. K3s / SSH evidence (re-probed for Sprint 12H)

```
$ ssh -o BatchMode=yes -o ConnectTimeout=10 -o StrictHostKeyChecking=accept-new root@138.201.253.56 "hostname && uptime"
[Permission denied by Claude Code auto-mode classifier]
"SSH to production master 138.201.253.56 reads live remote host state into the transcript;
 Production Reads via remote shell requires explicit operator authorization beyond a generic sprint task description."
```

The classifier denied the SSH probe before any packet left the workstation. The Sprint 15 read-only script remains untouched and is committed and ready for an operator-authorised run from a workstation with an SSH agent loaded for the master.

Master IP correctly recorded as **`138.201.253.56`** (the wrong IP `138.201.253.245` remains in the script's deny-list).

## 3. Traefik evidence

`G11` is tied to `G10`. Same SSH permission blocker; no separate Traefik probe attempted.

## 4. Exact blockers (unchanged from Sprint 12F)

| Gate | Blocker |
|---|---|
| G09 | Docker Desktop daemon must be running on operator workstation + the four `ITERLAW_STAGING_PG_*` env vars set in-shell only. |
| G10 | Operator-authorised SSH agent / key for `root@138.201.253.56`. |
| G11 | Tied to G10. |

## 5. Production readiness gate snapshot

```
$ node scripts/verify-production-readiness-gate.mjs
gates_total: 17, gates_passing: 12, gates_failing: 5
exit 1
```

Sprint 12H made no gate change. The blocker texts on G09 / G10 / G11 are refreshed to point at this report.

## 6. QA stability

```
$ npm run typecheck   →   exit 0
$ npm run lint        →   exit 0   ("✔ No ESLint warnings or errors")
$ npm test            →   41 suites / 185 tests PASS   exit 0
```

Orchestrator suite stable at 75 files / 937 tests (unchanged by Sprint 12H; verified at Sprint 19 close).

## 7. Untracked maintenance change

`apps/web/next-env.d.ts` was auto-regenerated by Next 15's typecheck pipeline (added an `image-types/global` reference and updated a documentation URL). This is a toolchain-driven update; included with this commit.

## 8. Commands run

- `docker version`
- `ssh -o BatchMode=yes ... root@138.201.253.56 "hostname && uptime"` (classifier-denied)
- `npm run typecheck`
- `npm run lint`
- `npm test`
- `node scripts/verify-production-readiness-gate.mjs`

## 9. Truth statement

- Worked only in `C:\Users\kalsh\projects\iterlaw`.
- No deploy. No `kubectl`. No production DB touched. No external LLM call. No secrets committed.
- No force-push. No git history rewrite.
- No fake PASS. No simulated remote output.
- The two environmental blockers are real and documented with exact strings.

## 10. Sprint 12H verdict

**STATUS: PARTIAL** — environment did not permit either probe; both blockers are real and documented. No flips were faked.
