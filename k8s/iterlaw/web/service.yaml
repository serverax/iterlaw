apiVersion: v1
kind: Service
metadata:
  name: iterlaw-web
  namespace: iterlaw-ai
  labels:
    app.kubernetes.io/name: iterlaw-web
    app.kubernetes.io/part-of: iterlaw
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: iterlaw-web
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
