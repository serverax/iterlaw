apiVersion: v1
kind: Service
metadata:
  name: legal-orchestrator
  namespace: iterlaw-ai
spec:
  selector:
    app: legal-orchestrator
  ports:
    - name: http
      port: 80
      targetPort: 3012
  type: ClusterIP
