PORT=4000
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Phase 1 — after applying lib/supabase/migrations/012-phase1-controlled-answer-engine.sql
# and inserting approved qa_pool / trusted_content rows, run:
#   npm run dev
# then: POST http://localhost:4000/ask  { "question": "..." }
#
# CI-style verification without a live project (mock Supabase + supertest):
#   npm run test:phase1
